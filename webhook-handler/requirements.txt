# fastapi pinned >=0.115.2 so a patched Starlette (>=0.40) is pulled —
# CVE-2024-47874 (multipart/form-data DoS); reachable via Slack form posts.
fastapi==0.115.6
uvicorn==0.27.0
httpx==0.26.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv==1.0.0
# >=0.0.18 fixes CVE-2024-24762 (ReDoS) and CVE-2024-53981 (multipart DoS).
python-multipart==0.0.27
apscheduler==3.10.4
PyNaCl==1.5.0
discord.py[voice]>=2.3.0
davey>=0.1.4
# vocolboy fork, NOT upstream: upstream's PacketRouter thread dies on the
# first corrupted opus packet (live 2026-06-12 11:51 — instant permanent
# deafness); the fork tolerates corruption (storms are contained in
# voice_bot.py and deaf sessions self-heal via the watchdog).
discord-ext-voice-recv @ git+https://github.com/vocolboy/discord-ext-voice-recv.git@main
elevenlabs>=2.39.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.20.0
